#Config File for shadowrun bot
#Ignores any line with a hash mark, stored internally as a list of key/value pairs
command_prefix=!
channel_name=general
#put in your custom bot token here. Bot will not work without it
bot_token=<replace with bot token>
