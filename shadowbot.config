#Config File for shadowrun bot
#Ignores any line with a hash mark, stored internally as a list of key/value pairs
command_prefix=!
channel_name=general
#put in your custom bot token here. Bot will not work without it
bot_token=<replace with bot token>
#TODO I have called DIR on many different objects none of them contain the is_owner function
#So we have to code it it manually. This param is optional, if not provided the exit/other owner only commands will not work.
author_id=<replace with author ID>
